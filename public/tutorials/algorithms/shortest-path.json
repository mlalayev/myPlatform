{
  "az": {
    "title": "Ən qısa yol alqoritmləri",
    "icon": "FiMap",
    "language": "multiple",
    "description": "Dijkstra, Bellman-Ford və digər ən qısa yol alqoritmləri çəkili qraflarda ən qısa yolu tapmaq üçün istifadə olunur, Dijkstra ilə ən qısa yol problemi ilə izah edilir.",
    "codes": {
      "c": "#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n\n#define MAX 100\n\nvoid dijkstra(int graph[MAX][MAX], int vertices, int start, int target) {\n  int dist[MAX];\n  int visited[MAX] = {0};\n  for (int i = 0; i < vertices; i++) {\n    dist[i] = INT_MAX;\n  }\n  dist[start] = 0;\n  for (int count = 0; count < vertices - 1; count++) {\n    int min = INT_MAX, u;\n    for (int i = 0; i < vertices; i++) {\n      if (!visited[i] && dist[i] <= min) {\n        min = dist[i];\n        u = i;\n      }\n    }\n    visited[u] = 1;\n    for (int v = 0; v < vertices; v++) {\n      if (!visited[v] && graph[u][v] && dist[u] != INT_MAX && dist[u] + graph[u][v] < dist[v]) {\n        dist[v] = dist[u] + graph[u][v];\n      }\n    }\n  }\n  if (dist[target] == INT_MAX) {\n    printf(\"Target not reachable\\n\");\n  } else {\n    printf(\"Distance to target: %d\\n\", dist[target]);\n  }\n}\n\nint main() {\n  int vertices = 5;\n  int graph[MAX][MAX] = {\n    {0, 4, 2, 0, 0},\n    {4, 0, 5, 10, 0},\n    {2, 5, 0, 3, 8},\n    {0, 10, 3, 0, 6},\n    {0, 0, 8, 6, 0}\n  };\n  dijkstra(graph, vertices, 0, 4);\n  return 0;\n}\n// Output: Distance to target: 8",
      "cpp": "#include <iostream>\n#include <vector>\n#include <climits>\n\nvoid dijkstra(std::vector<std::vector<int>>& graph, int start, int target) {\n  int vertices = graph.size();\n  std::vector<int> dist(vertices, INT_MAX);\n  std::vector<bool> visited(vertices, false);\n  dist[start] = 0;\n  for (int count = 0; count < vertices - 1; count++) {\n    int min = INT_MAX, u;\n    for (int i = 0; i < vertices; i++) {\n      if (!visited[i] && dist[i] <= min) {\n        min = dist[i];\n        u = i;\n      }\n    }\n    visited[u] = true;\n    for (int v = 0; v < vertices; v++) {\n      if (!visited[v] && graph[u][v] && dist[u] != INT_MAX && dist[u] + graph[u][v] < dist[v]) {\n        dist[v] = dist[u] + graph[u][v];\n      }\n    }\n  }\n  if (dist[target] == INT_MAX) {\n    std::cout << \"Target not reachable\" << std::endl;\n  } else {\n    std::cout << \"Distance to target: \" << dist[target] << std::endl;\n  }\n}\n\nint main() {\n  std::vector<std::vector<int>> graph = {\n    {0, 4, 2, 0, 0},\n    {4, 0, 5, 10, 0},\n    {2, 5, 0, 3, 8},\n    {0, 10, 3, 0, 6},\n    {0, 0, 8, 6, 0}\n  };\n  dijkstra(graph, 0, 4);\n  return 0;\n}\n// Output: Distance to target: 8",
      "csharp": "using System;\n\nclass Program {\n  static void Dijkstra(int[,] graph, int vertices, int start, int target) {\n    int[] dist = new int[vertices];\n    bool[] visited = new bool[vertices];\n    for (int i = 0; i < vertices; i++) {\n      dist[i] = int.MaxValue;\n    }\n    dist[start] = 0;\n    for (int count = 0; count < vertices - 1; count++) {\n      int min = int.MaxValue, u = 0;\n      for (int i = 0; i < vertices; i++) {\n        if (!visited[i] && dist[i] <= min) {\n          min = dist[i];\n          u = i;\n        }\n      }\n      visited[u] = true;\n      for (int v = 0; v < vertices; v++) {\n        if (!visited[v] && graph[u, v] != 0 && dist[u] != int.MaxValue && dist[u] + graph[u, v] < dist[v]) {\n          dist[v] = dist[u] + graph[u, v];\n        }\n      }\n    }\n    if (dist[target] == int.MaxValue) {\n      Console.WriteLine(\"Target not reachable\");\n    } else {\n      Console.WriteLine(\"Distance to target: \" + dist[target]);\n    }\n  }\n\n  static void Main() {\n    int vertices = 5;\n    int[,] graph = new int[,] {\n      {0, 4, 2, 0, 0},\n      {4, 0, 5, 10, 0},\n      {2, 5, 0, 3, 8},\n      {0, 10, 3, 0, 6},\n      {0, 0, 8, 6, 0}\n    };\n    Dijkstra(graph, vertices, 0, 4);\n  }\n}\n// Output: Distance to target: 8",
      "java": "import java.util.Arrays;\n\npublic class Main {\n  static void dijkstra(int[][] graph, int start, int target) {\n    int vertices = graph.length;\n    int[] dist = new int[vertices];\n    boolean[] visited = new boolean[vertices];\n    Arrays.fill(dist, Integer.MAX_VALUE);\n    dist[start] = 0;\n    for (int count = 0; count < vertices - 1; count++) {\n      int min = Integer.MAX_VALUE, u = 0;\n      for (int i = 0; i < vertices; i++) {\n        if (!visited[i] && dist[i] <= min) {\n          min = dist[i];\n          u = i;\n        }\n      }\n      visited[u] = true;\n      for (int v = 0; v < vertices; v++) {\n        if (!visited[v] && graph[u][v] != 0 && dist[u] != Integer.MAX_VALUE && dist[u] + graph[u][v] < dist[v]) {\n          dist[v] = dist[u] + graph[u][v];\n        }\n      }\n    }\n    if (dist[target] == Integer.MAX_VALUE) {\n      System.out.println(\"Target not reachable\");\n    } else {\n      System.out.println(\"Distance to target: \" + dist[target]);\n    }\n  }\n\n  public static void main(String[] args) {\n    int[][] graph = {\n      {0, 4, 2, 0, 0},\n      {4, 0, 5, 10, 0},\n      {2, 5, 0, 3, 8},\n      {0, 10, 3, 0, 6},\n      {0, 0, 8, 6, 0}\n    };\n    dijkstra(graph, 0, 4);\n  }\n}\n// Output: Distance to target: 8",
      "javascript": "function dijkstra(graph, start, target) {\n  const vertices = graph.length;\n  const dist = new Array(vertices).fill(Infinity);\n  const visited = new Array(vertices).fill(false);\n  dist[start] = 0;\n  for (let count = 0; count < vertices - 1; count++) {\n    let min = Infinity, u;\n    for (let i = 0; i < vertices; i++) {\n      if (!visited[i] && dist[i] <= min) {\n        min = dist[i];\n        u = i;\n      }\n    }\n    visited[u] = true;\n    for (let v = 0; v < vertices; v++) {\n      if (!visited[v] && graph[u][v] && dist[u] !== Infinity && dist[u] + graph[u][v] < dist[v]) {\n        dist[v] = dist[u] + graph[u][v];\n      }\n    }\n  }\n  if (dist[target] === Infinity) {\n    console.log(\"Target not reachable\");\n  } else {\n    console.log(`Distance to target: ${dist[target]}`);\n  }\n}\n\nconst graph = [\n  [0, 4, 2, 0, 0],\n  [4, 0, 5, 10, 0],\n  [2, 5, 0, 3, 8],\n  [0, 10, 3, 0, 6],\n  [0, 0, 8, 6, 0]\n];\ndijkstra(graph, 0, 4);\n// Output: Distance to target: 8",
      "typescript": "function dijkstra(graph: number[][], start: number, target: number): void {\n  const vertices: number = graph.length;\n  const dist: number[] = new Array(vertices).fill(Infinity);\n  const visited: boolean[] = new Array(vertices).fill(false);\n  dist[start] = 0;\n  for (let count = 0; count < vertices - 1; count++) {\n    let min: number = Infinity, u: number = 0;\n    for (let i = 0; i < vertices; i++) {\n      if (!visited[i] && dist[i] <= min) {\n        min = dist[i];\n        u = i;\n      }\n    }\n    visited[u] = true;\n    for (let v = 0; v < vertices; v++) {\n      if (!visited[v] && graph[u][v] && dist[u] !== Infinity && dist[u] + graph[u][v] < dist[v]) {\n        dist[v] = dist[u] + graph[u][v];\n      }\n    }\n  }\n  if (dist[target] === Infinity) {\n    console.log(\"Target not reachable\");\n  } else {\n    console.log(`Distance to target: ${dist[target]}`);\n  }\n}\n\nconst graph: number[][] = [\n  [0, 4, 2, 0, 0],\n  [4, 0, 5, 10, 0],\n  [2, 5, 0, 3, 8],\n  [0, 10, 3, 0, 6],\n  [0, 0, 8, 6, 0]\n];\ndijkstra(graph, 0, 4);\n// Output: Distance to target: 8",
      "python": "def dijkstra(graph, start, target):\n    vertices = len(graph)\n    dist = [float('inf')] * vertices\n    visited = [False] * vertices\n    dist[start] = 0\n    for count in range(vertices - 1):\n        min_dist, u = float('inf'), 0\n        for i in range(vertices):\n            if not visited[i] and dist[i] <= min_dist:\n                min_dist, u = dist[i], i\n        visited[u] = True\n        for v in range(vertices):\n            if not visited[v] and graph[u][v] and dist[u] != float('inf') and dist[u] + graph[u][v] < dist[v]:\n                dist[v] = dist[u] + graph[u][v]\n    if dist[target] == float('inf'):\n        print(\"Target not reachable\")\n    else:\n        print(f\"Distance to target: {dist[target]}\")\n\ngraph = [\n    [0, 4, 2, 0, 0],\n    [4, 0, 5, 10, 0],\n    [2, 5, 0, 3, 8],\n    [0, 10, 3, 0, 6],\n    [0, 0, 8, 6, 0]\n]\ndijkstra(graph, 0, 4)\n# Output: Distance to target: 8",
      "php": "<?php\n\nfunction dijkstra($graph, $start, $target) {\n  $vertices = count($graph);\n  $dist = array_fill(0, $vertices, PHP_INT_MAX);\n  $visited = array_fill(0, $vertices, false);\n  $dist[$start] = 0;\n  for ($count = 0; $count < $vertices - 1; $count++) {\n    $min = PHP_INT_MAX;\n    $u = 0;\n    for ($i = 0; $i < $vertices; $i++) {\n      if (!$visited[$i] && $dist[$i] <= $min) {\n        $min = $dist[$i];\n        $u = $i;\n      }\n    }\n    $visited[$u] = true;\n    for ($v = 0; $v < $vertices; $v++) {\n      if (!$visited[$v] && $graph[$u][$v] && $dist[$u] != PHP_INT_MAX && $dist[$u] + $graph[$u][$v] < $dist[$v]) {\n        $dist[$v] = $dist[$u] + $graph[$u][$v];\n      }\n    }\n  }\n  if ($dist[$target] == PHP_INT_MAX) {\n    echo \"Target not reachable\\n\";\n  } else {\n    echo \"Distance to target: \" . $dist[$target] . \"\\n\";\n  }\n}\n\n$graph = [\n  [0, 4, 2, 0, 0],\n  [4, 0, 5, 10, 0],\n  [2, 5, 0, 3, 8],\n  [0, 10, 3, 0, 6],\n  [0, 0, 8, 6, 0]\n];\ndijkstra($graph, 0, 4);\n// Output: Distance to target: 8"
    },
    "content": [
      {
        "type": "heading",
        "text": "Ən qısa yol alqoritmləri nədir?"
      },
      {
        "type": "paragraph",
        "text": "Ən qısa yol alqoritmləri çəkili qraflarda iki nöqtə arasında ən qısa yolu tapmaq üçün istifadə olunur. Qraf nöqtələr (vertices) və onların arasındakı çəkili kənarlardan (edges) ibarətdir, məsələn, şəhərləri və yolların məsafələrini təmsil edə bilər. Ən məşhur alqoritmlər Dijkstra və Bellman-Ford-dur. Dijkstra alqoritmi müsbət çəkili qraflarda ən qısa yolu tapır və açgözlü (greedy) strategiyadan istifadə edir. Bellman-Ford isə mənfi çəkiləri də dəstəkləyir və mənfi dövrü aşkarlaya bilir. Məsələn, Dijkstra ilə bir şəhərdən digərinə ən qısa məsafə tapıla bilər (məsələn, 0-dan 4-ə 8 vahid məsafə). Backtracking ilə müqayisədə ən qısa yol alqoritmləri daha səmərəlidir, çünki bütün mümkün yolları yoxlamır. Dinamik proqramlaşdırma ilə müqayisədə isə Dijkstra daha az yaddaş tələb edir, lakin Bellman-Ford daha çox hesablama tələb edə bilər."
      },
      {
        "type": "heading",
        "text": "Ən qısa yol alqoritmləri necə işləyir?"
      },
      {
        "type": "paragraph",
        "text": "Dijkstra alqoritmi belə işləyir: 1) **Başlanğıc nöqtə** - axtarış başlanğıc nöqtəsindən başlayır (məsələn, 0). 2) **Məsafə massivi** - hər nöqtəyə ən qısa məsafəni saxlamaq üçün massiv yaradılır, başlanğıc nöqtəsinin məsafəsi 0, digərləri sonsuzdur. 3) **Açgözlü seçim** - hər addımda ən kiçik məsafəyə malik ziyarət edilməmiş nöqtə seçilir. 4) **Məsafə yenilənməsi** - seçilmiş nöqtənin qonşularına gedən məsafələr yenilənir, əgər daha qısa yol tapılırsa. Məsələn, 5 nöqtəli qrafda 0-dan 4-ə yol 0 → 2 → 4 (məsafə 8) ola bilər. Bellman-Ford isə bütün kənarları dəfələrlə yoxlayır və mənfi çəkiləri də nəzərə alır. Dijkstra müsbət çəkili qraflarda daha sürətlidir, Bellman-Ford isə mənfi çəkilər və dövr aşkarlamaq üçün uyğundur."
      },
      {
        "type": "heading",
        "text": "Ən qısa yol alqoritmlərinin səmərəliliyi (Big O)"
      },
      {
        "type": "paragraph",
        "text": "Dijkstra alqoritminin səmərəliliyi qrafın təmsilindən asılıdır. Qonşuluq matrisi ilə vaxt mürəkkəbliyi O(V²)-dir, burada V nöqtələrin sayıdır, çünki hər nöqtənin qonşuları yoxlanılır. Prioritet növbəsi (heap) ilə O((V + E) log V) olur, burada E kənarların sayıdır. Məsələn, 5 nöqtəli və 7 kənarlı qrafda Dijkstra O(25) və ya heap ilə O(12 log 5) əməliyyat aparır. Bellman-Ford-un vaxt mürəkkəbliyi O(V * E)-dir, bu da böyük qraflarda yavaş ola bilər. Məkan mürəkkəbliyi hər iki alqoritmdə O(V)-dir (məsafə və visited massivləri üçün). Backtracking ilə müqayisədə (məsələn, O(N!) N-Queens üçün), ən qısa yol alqoritmləri daha səmərəlidir. Dinamik proqramlaşdırma ilə müqayisədə (məsələn, O(n * m) Minimum sikkə problemində) Dijkstra daha az yaddaş tələb edir, lakin Bellman-Ford daha çox hesablama tələb edə bilər."
      },
      {
        "type": "heading",
        "text": "Ən qısa yol alqoritmlərinin üstünlükləri və mənfi cəhətləri"
      },
      {
        "type": "list",
        "items": [
          {
            "description": "Effektivlik: Dijkstra müsbət çəkili qraflarda sürətli ən qısa yolu tapır."
          },
          {
            "description": "Çox yönlülük: Bellman-Ford mənfi çəkiləri və dövr aşkarlamasını dəstəkləyir."
          },
          { "description": "Mənfi cəhət: Dijkstra mənfi çəkilərdə işləmir." },
          {
            "description": "Mənfi cəhət: Bellman-Ford böyük qraflarda yavaşdır (O(V * E))."
          }
        ]
      },
      {
        "type": "heading",
        "text": "Real həyatda ən qısa yol alqoritmləri"
      },
      {
        "type": "paragraph",
        "text": "Ən qısa yol alqoritmləri naviqasiya sistemlərində (məsələn, Google Maps-də iki nöqtə arasında ən qısa marşrut tapmaq), şəbəkə marşrutlaşdırmasında (məsələn, internetdə məlumat paketlərinin ən qısa yolla göndərilməsi) və logistikada (məsələn, çatdırılma marşrutlarının optimallaşdırılması) geniş istifadə olunur. Dijkstra alqoritmi müsbət çəkili qraflarda, məsələn, şəhərlər arası yol məsafələrində effektivdir. Bellman-Ford isə mənfi çəkilərin olduğu ssenarilərdə, məsələn, valyuta mübadiləsi arbitrajında istifadə olunur. Backtracking ilə müqayisədə ən qısa yol alqoritmləri daha səmərəlidir, çünki bütün mümkün yolları yoxlamır. Dinamik proqramlaşdırma ilə müqayisədə Dijkstra daha az yaddaş tələb edir, lakin Bellman-Ford daha çox hesablama tələb edə bilər. Bu alqoritmlər müasir texnologiyalarda, məsələn, GPS və şəbəkə təhlükəsizliyində vacibdir."
      },
      {
        "type": "heading",
        "text": "Təhlükələr və tövsiyələr"
      },
      {
        "type": "paragraph",
        "text": "Ən qısa yol alqoritmlərinin əsas təhlükəsi böyük qraflarda hesablama yüküdür. Dijkstra ilə qonşuluq matrisi istifadə edildikdə O(V²) vaxt tələb olunur, Bellman-Ford isə O(V * E) ilə daha yavaş ola bilər. Böyük qraflarda yaddaş istifadəsi də problem yarada bilər. Tövsiyələrə gəlincə, Dijkstra üçün prioritet növbəsi (heap) istifadə edərək O((V + E) log V) səmərəliliyinə nail olmaq mümkündür. Mənfi çəkilər varsa, Bellman-Ford seçilməlidir. Problemin xüsusiyyətlərinə uyğun alqoritm seçilməli, məsələn, müsbət çəkilər üçün Dijkstra, mənfi çəkilər və ya dövr aşkarlamaq üçün Bellman-Ford uyğundur. Alqoritmi müxtəlif ölçülü qraflarla sınaqdan keçirin və performansını qiymətləndirin. Əgər sürət prioritetdirsə, A* kimi heuristik alqoritmlər araşdırıla bilər, lakin onların optimal həll təmin etdiyi yoxlanılmalıdır."
      }
    ]
  },
  "en": {
    "title": "Shortest Path Algorithms",
    "icon": "FiMap",
    "language": "multiple",
    "description": "Dijkstra, Bellman-Ford, and other shortest path algorithms find the shortest path in weighted graphs, exemplified by Dijkstra for shortest path finding.",
    "codes": {
      "c": "#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n\n#define MAX 100\n\nvoid dijkstra(int graph[MAX][MAX], int vertices, int start, int target) {\n  int dist[MAX];\n  int visited[MAX] = {0};\n  for (int i = 0; i < vertices; i++) {\n    dist[i] = INT_MAX;\n  }\n  dist[start] = 0;\n  for (int count = 0; count < vertices - 1; count++) {\n    int min = INT_MAX, u;\n    for (int i = 0; i < vertices; i++) {\n      if (!visited[i] && dist[i] <= min) {\n        min = dist[i];\n        u = i;\n      }\n    }\n    visited[u] = 1;\n    for (int v = 0; v < vertices; v++) {\n      if (!visited[v] && graph[u][v] && dist[u] != INT_MAX && dist[u] + graph[u][v] < dist[v]) {\n        dist[v] = dist[u] + graph[u][v];\n      }\n    }\n  }\n  if (dist[target] == INT_MAX) {\n    printf(\"Target not reachable\\n\");\n  } else {\n    printf(\"Distance to target: %d\\n\", dist[target]);\n  }\n}\n\nint main() {\n  int vertices = 5;\n  int graph[MAX][MAX] = {\n    {0, 4, 2, 0, 0},\n    {4, 0, 5, 10, 0},\n    {2, 5, 0, 3, 8},\n    {0, 10, 3, 0, 6},\n    {0, 0, 8, 6, 0}\n  };\n  dijkstra(graph, vertices, 0, 4);\n  return 0;\n}\n// Output: Distance to target: 8",
      "cpp": "#include <iostream>\n#include <vector>\n#include <climits>\n\nvoid dijkstra(std::vector<std::vector<int>>& graph, int start, int target) {\n  int vertices = graph.size();\n  std::vector<int> dist(vertices, INT_MAX);\n  std::vector<bool> visited(vertices, false);\n  dist[start] = 0;\n  for (int count = 0; count < vertices - 1; count++) {\n    int min = INT_MAX, u;\n    for (int i = 0; i < vertices; i++) {\n      if (!visited[i] && dist[i] <= min) {\n        min = dist[i];\n        u = i;\n      }\n    }\n    visited[u] = true;\n    for (int v = 0; v < vertices; v++) {\n      if (!visited[v] && graph[u][v] && dist[u] != INT_MAX && dist[u] + graph[u][v] < dist[v]) {\n        dist[v] = dist[u] + graph[u][v];\n      }\n    }\n  }\n  if (dist[target] == INT_MAX) {\n    std::cout << \"Target not reachable\" << std::endl;\n  } else {\n    std::cout << \"Distance to target: \" << dist[target] << std::endl;\n  }\n}\n\nint main() {\n  std::vector<std::vector<int>> graph = {\n    {0, 4, 2, 0, 0},\n    {4, 0, 5, 10, 0},\n    {2, 5, 0, 3, 8},\n    {0, 10, 3, 0, 6},\n    {0, 0, 8, 6, 0}\n  };\n  dijkstra(graph, 0, 4);\n  return 0;\n}\n// Output: Distance to target: 8",
      "csharp": "using System;\n\nclass Program {\n  static void Dijkstra(int[,] graph, int vertices, int start, int target) {\n    int[] dist = new int[vertices];\n    bool[] visited = new bool[vertices];\n    for (int i = 0; i < vertices; i++) {\n      dist[i] = int.MaxValue;\n    }\n    dist[start] = 0;\n    for (int count = 0; count < vertices - 1; count++) {\n      int min = int.MaxValue, u = 0;\n      for (int i = 0; i < vertices; i++) {\n        if (!visited[i] && dist[i] <= min) {\n          min = dist[i];\n          u = i;\n        }\n      }\n      visited[u] = true;\n      for (int v = 0; v < vertices; v++) {\n        if (!visited[v] && graph[u, v] != 0 && dist[u] != int.MaxValue && dist[u] + graph[u, v] < dist[v]) {\n          dist[v] = dist[u] + graph[u, v];\n        }\n      }\n    }\n    if (dist[target] == int.MaxValue) {\n      Console.WriteLine(\"Target not reachable\");\n    } else {\n      Console.WriteLine(\"Distance to target: \" + dist[target]);\n    }\n  }\n\n  static void Main() {\n    int vertices = 5;\n    int[,] graph = new int[,] {\n      {0, 4, 2, 0, 0},\n      {4, 0, 5, 10, 0},\n      {2, 5, 0, 3, 8},\n      {0, 10, 3, 0, 6},\n      {0, 0, 8, 6, 0}\n    };\n    Dijkstra(graph, vertices, 0, 4);\n  }\n}\n// Output: Distance to target: 8",
      "java": "import java.util.Arrays;\n\npublic class Main {\n  static void dijkstra(int[][] graph, int start, int target) {\n    int vertices = graph.length;\n    int[] dist = new int[vertices];\n    boolean[] visited = new boolean[vertices];\n    Arrays.fill(dist, Integer.MAX_VALUE);\n    dist[start] = 0;\n    for (int count = 0; count < vertices - 1; count++) {\n      int min = Integer.MAX_VALUE, u = 0;\n      for (int i = 0; i < vertices; i++) {\n        if (!visited[i] && dist[i] <= min) {\n          min = dist[i];\n          u = i;\n        }\n      }\n      visited[u] = true;\n      for (int v = 0; v < vertices; v++) {\n        if (!visited[v] && graph[u][v] != 0 && dist[u] != Integer.MAX_VALUE && dist[u] + graph[u][v] < dist[v]) {\n          dist[v] = dist[u] + graph[u][v];\n        }\n      }\n    }\n    if (dist[target] == Integer.MAX_VALUE) {\n      System.out.println(\"Target not reachable\");\n    } else {\n      System.out.println(\"Distance to target: \" + dist[target]);\n    }\n  }\n\n  public static void main(String[] args) {\n    int[][] graph = {\n      {0, 4, 2, 0, 0},\n      {4, 0, 5, 10, 0},\n      {2, 5, 0, 3, 8},\n      {0, 10, 3, 0, 6},\n      {0, 0, 8, 6, 0}\n    };\n    dijkstra(graph, 0, 4);\n  }\n}\n// Output: Distance to target: 8",
      "javascript": "function dijkstra(graph, start, target) {\n  const vertices = graph.length;\n  const dist = new Array(vertices).fill(Infinity);\n  const visited = new Array(vertices).fill(false);\n  dist[start] = 0;\n  for (let count = 0; count < vertices - 1; count++) {\n    let min = Infinity, u;\n    for (let i = 0; i < vertices; i++) {\n      if (!visited[i] && dist[i] <= min) {\n        min = dist[i];\n        u = i;\n      }\n    }\n    visited[u] = true;\n    for (let v = 0; v < vertices; v++) {\n      if (!visited[v] && graph[u][v] && dist[u] !== Infinity && dist[u] + graph[u][v] < dist[v]) {\n        dist[v] = dist[u] + graph[u][v];\n      }\n    }\n  }\n  if (dist[target] === Infinity) {\n    console.log(\"Target not reachable\");\n  } else {\n    console.log(`Distance to target: ${dist[target]}`);\n  }\n}\n\nconst graph = [\n  [0, 4, 2, 0, 0],\n  [4, 0, 5, 10, 0],\n  [2, 5, 0, 3, 8],\n  [0, 10, 3, 0, 6],\n  [0, 0, 8, 6, 0]\n];\ndijkstra(graph, 0, 4);\n// Output: Distance to target: 8",
      "typescript": "function dijkstra(graph: number[][], start: number, target: number): void {\n  const vertices: number = graph.length;\n  const dist: number[] = new Array(vertices).fill(Infinity);\n  const visited: boolean[] = new Array(vertices).fill(false);\n  dist[start] = 0;\n  for (let count = 0; count < vertices - 1; count++) {\n    let min: number = Infinity, u: number = 0;\n    for (let i = 0; i < vertices; i++) {\n      if (!visited[i] && dist[i] <= min) {\n        min = dist[i];\n        u = i;\n      }\n    }\n    visited[u] = true;\n    for (let v = 0; v < vertices; v++) {\n      if (!visited[v] && graph[u][v] && dist[u] !== Infinity && dist[u] + graph[u][v] < dist[v]) {\n        dist[v] = dist[u] + graph[u][v];\n      }\n    }\n  }\n  if (dist[target] === Infinity) {\n    console.log(\"Target not reachable\");\n  } else {\n    console.log(`Distance to target: ${dist[target]}`);\n  }\n}\n\nconst graph: number[][] = [\n  [0, 4, 2, 0, 0],\n  [4, 0, 5, 10, 0],\n  [2, 5, 0, 3, 8],\n  [0, 10, 3, 0, 6],\n  [0, 0, 8, 6, 0]\n];\ndijkstra(graph, 0, 4);\n// Output: Distance to target: 8",
      "python": "def dijkstra(graph, start, target):\n    vertices = len(graph)\n    dist = [float('inf')] * vertices\n    visited = [False] * vertices\n    dist[start] = 0\n    for count in range(vertices - 1):\n        min_dist, u = float('inf'), 0\n        for i in range(vertices):\n            if not visited[i] and dist[i] <= min_dist:\n                min_dist, u = dist[i], i\n        visited[u] = True\n        for v in range(vertices):\n            if not visited[v] and graph[u][v] and dist[u] != float('inf') and dist[u] + graph[u][v] < dist[v]:\n                dist[v] = dist[u] + graph[u][v]\n    if dist[target] == float('inf'):\n        print(\"Target not reachable\")\n    else:\n        print(f\"Distance to target: {dist[target]}\")\n\ngraph = [\n    [0, 4, 2, 0, 0],\n    [4, 0, 5, 10, 0],\n    [2, 5, 0, 3, 8],\n    [0, 10, 3, 0, 6],\n    [0, 0, 8, 6, 0]\n]\ndijkstra(graph, 0, 4)\n# Output: Distance to target: 8",
      "php": "<?php\n\nfunction dijkstra($graph, $start, $target) {\n  $vertices = count($graph);\n  $dist = array_fill(0, $vertices, PHP_INT_MAX);\n  $visited = array_fill(0, $vertices, false);\n  $dist[$start] = 0;\n  for ($count = 0; $count < $vertices - 1; $count++) {\n    $min = PHP_INT_MAX;\n    $u = 0;\n    for ($i = 0; $i < $vertices; $i++) {\n      if (!$visited[$i] && $dist[$i] <= $min) {\n        $min = $dist[$i];\n        $u = $i;\n      }\n    }\n    $visited[$u] = true;\n    for ($v = 0; $v < $vertices; $v++) {\n      if (!$visited[$v] && $graph[$u][$v] && $dist[$u] != PHP_INT_MAX && $dist[$u] + $graph[$u][$v] < $dist[$v]) {\n        $dist[$v] = $dist[$u] + $graph[$u][$v];\n      }\n    }\n  }\n  if ($dist[$target] == PHP_INT_MAX) {\n    echo \"Target not reachable\\n\";\n  } else {\n    echo \"Distance to target: \" . $dist[$target] . \"\\n\";\n  }\n}\n\n$graph = [\n  [0, 4, 2, 0, 0],\n  [4, 0, 5, 10, 0],\n  [2, 5, 0, 3, 8],\n  [0, 10, 3, 0, 6],\n  [0, 0, 8, 6, 0]\n];\ndijkstra($graph, 0, 4);\n// Output: Distance to target: 8"
    },
    "content": [
      {
        "type": "heading",
        "text": "What are Shortest Path Algorithms?"
      },
      {
        "type": "paragraph",
        "text": "Shortest path algorithms find the shortest path between two nodes in a weighted graph, where edges have weights (e.g., representing distances between cities). A graph consists of nodes (vertices) and weighted edges. The most well-known algorithms are Dijkstra and Bellman-Ford. Dijkstra’s algorithm finds the shortest path in graphs with non-negative weights using a greedy approach. Bellman-Ford handles negative weights and can detect negative cycles. For example, Dijkstra can find the shortest distance between two cities (e.g., from node 0 to node 4, distance 8). Compared to backtracking, shortest path algorithms are more efficient as they don’t explore all possible paths. Compared to dynamic programming, Dijkstra uses less memory, but Bellman-Ford may require more computation."
      },
      {
        "type": "heading",
        "text": "How Do Shortest Path Algorithms Work?"
      },
      {
        "type": "paragraph",
        "text": "Dijkstra’s algorithm works as follows: 1) **Start node** - the search begins at the start node (e.g., node 0). 2) **Distance array** - an array stores the shortest distances to each node, with the start node’s distance set to 0 and others to infinity. 3) **Greedy selection** - at each step, the unvisited node with the smallest distance is selected. 4) **Distance update** - distances to the selected node’s neighbors are updated if a shorter path is found. For example, in a 5-node graph, Dijkstra finds the path from node 0 to node 4 (e.g., 0 → 2 → 4, distance 8). Bellman-Ford iteratively checks all edges and handles negative weights. Dijkstra is faster for non-negative weights, while Bellman-Ford is suitable for negative weights and cycle detection."
      },
      {
        "type": "heading",
        "text": "Efficiency of Shortest Path Algorithms (Big O)"
      },
      {
        "type": "paragraph",
        "text": "The efficiency of Dijkstra’s algorithm depends on the graph representation. With an adjacency matrix, the time complexity is O(V²), where V is the number of vertices, as all neighbors are checked. With a priority queue (heap), it’s O((V + E) log V), where E is the number of edges. For example, in a 5-node, 7-edge graph, Dijkstra takes O(25) or O(12 log 5) operations with a heap. Bellman-Ford’s time complexity is O(V * E), which can be slow for large graphs. Space complexity for both is O(V) for distance and visited arrays. Compared to backtracking (e.g., O(N!) for N-Queens), shortest path algorithms are more efficient. Compared to dynamic programming (e.g., O(n * m) for Minimum Coin Change), Dijkstra uses less memory, but Bellman-Ford may require more computation."
      },
      {
        "type": "heading",
        "text": "Advantages and Disadvantages of Shortest Path Algorithms"
      },
      {
        "type": "list",
        "items": [
          {
            "description": "Efficiency: Dijkstra is fast for non-negative weighted graphs."
          },
          {
            "description": "Versatility: Bellman-Ford supports negative weights and cycle detection."
          },
          {
            "description": "Disadvantage: Dijkstra doesn’t work with negative weights."
          },
          {
            "description": "Disadvantage: Bellman-Ford is slow for large graphs (O(V * E))."
          }
        ]
      },
      {
        "type": "heading",
        "text": "Shortest Path Algorithms in Real Life"
      },
      {
        "type": "paragraph",
        "text": "Shortest path algorithms are widely used in navigation systems (e.g., Google Maps finding the shortest route between two points), network routing (e.g., sending data packets via the shortest path in the internet), and logistics (e.g., optimizing delivery routes). Dijkstra’s algorithm is effective for non-negative weighted graphs, such as road distances between cities. Bellman-Ford is used in scenarios with negative weights, like currency exchange arbitrage. Compared to backtracking, shortest path algorithms are more efficient as they don’t explore all paths. Compared to dynamic programming, Dijkstra uses less memory, but Bellman-Ford may require more computation. These algorithms are critical in modern technologies like GPS and network security."
      },
      {
        "type": "heading",
        "text": "Dangers and Recommendations"
      },
      {
        "type": "paragraph",
        "text": "The main danger of shortest path algorithms is the computational load for large graphs. Dijkstra with an adjacency matrix requires O(V²) time, and Bellman-Ford’s O(V * E) can be slower. Memory usage can also be an issue for large graphs. Recommendations include using a priority queue for Dijkstra to achieve O((V + E) log V) efficiency. Use Bellman-Ford for graphs with negative weights. Choose the algorithm based on the problem, e.g., Dijkstra for non-negative weights, Bellman-Ford for negative weights or cycle detection. Test the algorithm with different graph sizes to evaluate performance. If speed is a priority, explore heuristic algorithms like A*, but verify they provide optimal solutions."
      }
    ]
  },
  "ru": {
    "title": "Алгоритмы поиска кратчайшего пути",
    "icon": "FiMap",
    "language": "multiple",
    "description": "Dijkstra, Bellman-Ford и другие алгоритмы кратчайшего пути находят кратчайший путь в взвешенных графах, примером чего является Dijkstra для поиска кратчайшего пути.",
    "codes": {
      "c": "#include <stdio.h>\n#include <stdlib.h>\n#include <limits.h>\n\n#define MAX 100\n\nvoid dijkstra(int graph[MAX][MAX], int vertices, int start, int target) {\n  int dist[MAX];\n  int visited[MAX] = {0};\n  for (int i = 0; i < vertices; i++) {\n    dist[i] = INT_MAX;\n  }\n  dist[start] = 0;\n  for (int count = 0; count < vertices - 1; count++) {\n    int min = INT_MAX, u;\n    for (int i = 0; i < vertices; i++) {\n      if (!visited[i] && dist[i] <= min) {\n        min = dist[i];\n        u = i;\n      }\n    }\n    visited[u] = 1;\n    for (int v = 0; v < vertices; v++) {\n      if (!visited[v] && graph[u][v] && dist[u] != INT_MAX && dist[u] + graph[u][v] < dist[v]) {\n        dist[v] = dist[u] + graph[u][v];\n      }\n    }\n  }\n  if (dist[target] == INT_MAX) {\n    printf(\"Target not reachable\\n\");\n  } else {\n    printf(\"Distance to target: %d\\n\", dist[target]);\n  }\n}\n\nint main() {\n  int vertices = 5;\n  int graph[MAX][MAX] = {\n    {0, 4, 2, 0, 0},\n    {4, 0, 5, 10, 0},\n    {2, 5, 0, 3, 8},\n    {0, 10, 3, 0, 6},\n    {0, 0, 8, 6, 0}\n  };\n  dijkstra(graph, vertices, 0, 4);\n  return 0;\n}\n// Output: Distance to target: 8",
      "cpp": "#include <iostream>\n#include <vector>\n#include <climits>\n\nvoid dijkstra(std::vector<std::vector<int>>& graph, int start, int target) {\n  int vertices = graph.size();\n  std::vector<int> dist(vertices, INT_MAX);\n  std::vector<bool> visited(vertices, false);\n  dist[start] = 0;\n  for (int count = 0; count < vertices - 1; count++) {\n    int min = INT_MAX, u;\n    for (int i = 0; i < vertices; i++) {\n      if (!visited[i] && dist[i] <= min) {\n        min = dist[i];\n        u = i;\n      }\n    }\n    visited[u] = true;\n    for (int v = 0; v < vertices; v++) {\n      if (!visited[v] && graph[u][v] && dist[u] != INT_MAX && dist[u] + graph[u][v] < dist[v]) {\n        dist[v] = dist[u] + graph[u][v];\n      }\n    }\n  }\n  if (dist[target] == INT_MAX) {\n    std::cout << \"Target not reachable\" << std::endl;\n  } else {\n    std::cout << \"Distance to target: \" << dist[target] << std::endl;\n  }\n}\n\nint main() {\n  std::vector<std::vector<int>> graph = {\n    {0, 4, 2, 0, 0},\n    {4, 0, 5, 10, 0},\n    {2, 5, 0, 3, 8},\n    {0, 10, 3, 0, 6},\n    {0, 0, 8, 6, 0}\n  };\n  dijkstra(graph, 0, 4);\n  return 0;\n}\n// Output: Distance to target: 8",
      "csharp": "using System;\n\nclass Program {\n  static void Dijkstra(int[,] graph, int vertices, int start, int target) {\n    int[] dist = new int[vertices];\n    bool[] visited = new bool[vertices];\n    for (int i = 0; i < vertices; i++) {\n      dist[i] = int.MaxValue;\n    }\n    dist[start] = 0;\n    for (int count = 0; count < vertices - 1; count++) {\n      int min = int.MaxValue, u = 0;\n      for (int i = 0; i < vertices; i++) {\n        if (!visited[i] && dist[i] <= min) {\n          min = dist[i];\n          u = i;\n        }\n      }\n      visited[u] = true;\n      for (int v = 0; v < vertices; v++) {\n        if (!visited[v] && graph[u, v] != 0 && dist[u] != int.MaxValue && dist[u] + graph[u, v] < dist[v]) {\n          dist[v] = dist[u] + graph[u, v];\n        }\n      }\n    }\n    if (dist[target] == int.MaxValue) {\n      Console.WriteLine(\"Target not reachable\");\n    } else {\n      Console.WriteLine(\"Distance to target: \" + dist[target]);\n    }\n  }\n\n  static void Main() {\n    int vertices = 5;\n    int[,] graph = new int[,] {\n      {0, 4, 2, 0, 0},\n      {4, 0, 5, 10, 0},\n      {2, 5, 0, 3, 8},\n      {0, 10, 3, 0, 6},\n      {0, 0, 8, 6, 0}\n    };\n    Dijkstra(graph, vertices, 0, 4);\n  }\n}\n// Output: Distance to target: 8",
      "java": "import java.util.Arrays;\n\npublic class Main {\n  static void dijkstra(int[][] graph, int start, int target) {\n    int vertices = graph.length;\n    int[] dist = new int[vertices];\n    boolean[] visited = new boolean[vertices];\n    Arrays.fill(dist, Integer.MAX_VALUE);\n    dist[start] = 0;\n    for (int count = 0; count < vertices - 1; count++) {\n      int min = Integer.MAX_VALUE, u = 0;\n      for (int i = 0; i < vertices; i++) {\n        if (!visited[i] && dist[i] <= min) {\n          min = dist[i];\n          u = i;\n        }\n      }\n      visited[u] = true;\n      for (int v = 0; v < vertices; v++) {\n        if (!visited[v] && graph[u][v] != 0 && dist[u] != Integer.MAX_VALUE && dist[u] + graph[u][v] < dist[v]) {\n          dist[v] = dist[u] + graph[u][v];\n        }\n      }\n    }\n    if (dist[target] == Integer.MAX_VALUE) {\n      System.out.println(\"Target not reachable\");\n    } else {\n      System.out.println(\"Distance to target: \" + dist[target]);\n    }\n  }\n\n  public static void main(String[] args) {\n    int[][] graph = {\n      {0, 4, 2, 0, 0},\n      {4, 0, 5, 10, 0},\n      {2, 5, 0, 3, 8},\n      {0, 10, 3, 0, 6},\n      {0, 0, 8, 6, 0}\n    };\n    dijkstra(graph, 0, 4);\n  }\n}\n// Output: Distance to target: 8",
      "javascript": "function dijkstra(graph, start, target) {\n  const vertices = graph.length;\n  const dist = new Array(vertices).fill(Infinity);\n  const visited = new Array(vertices).fill(false);\n  dist[start] = 0;\n  for (let count = 0; count < vertices - 1; count++) {\n    let min = Infinity, u;\n    for (let i = 0; i < vertices; i++) {\n      if (!visited[i] && dist[i] <= min) {\n        min = dist[i];\n        u = i;\n      }\n    }\n    visited[u] = true;\n    for (let v = 0; v < vertices; v++) {\n      if (!visited[v] && graph[u][v] && dist[u] !== Infinity && dist[u] + graph[u][v] < dist[v]) {\n        dist[v] = dist[u] + graph[u][v];\n      }\n    }\n  }\n  if (dist[target] === Infinity) {\n    console.log(\"Target not reachable\");\n  } else {\n    console.log(`Distance to target: ${dist[target]}`);\n  }\n}\n\nconst graph = [\n  [0, 4, 2, 0, 0],\n  [4, 0, 5, 10, 0],\n  [2, 5, 0, 3, 8],\n  [0, 10, 3, 0, 6],\n  [0, 0, 8, 6, 0]\n];\ndijkstra(graph, 0, 4);\n// Output: Distance to target: 8",
      "typescript": "function dijkstra(graph: number[][], start: number, target: number): void {\n  const vertices: number = graph.length;\n  const dist: number[] = new Array(vertices).fill(Infinity);\n  const visited: boolean[] = new Array(vertices).fill(false);\n  dist[start] = 0;\n  for (let count = 0; count < vertices - 1; count++) {\n    let min: number = Infinity, u: number = 0;\n    for (let i = 0; i < vertices; i++) {\n      if (!visited[i] && dist[i] <= min) {\n        min = dist[i];\n        u = i;\n      }\n    }\n    visited[u] = true;\n    for (let v = 0; v < vertices; v++) {\n      if (!visited[v] && graph[u][v] && dist[u] !== Infinity && dist[u] + graph[u][v] < dist[v]) {\n        dist[v] = dist[u] + graph[u][v];\n      }\n    }\n  }\n  if (dist[target] === Infinity) {\n    console.log(\"Target not reachable\");\n  } else {\n    console.log(`Distance to target: ${dist[target]}`);\n  }\n}\n\nconst graph: number[][] = [\n  [0, 4, 2, 0, 0],\n  [4, 0, 5, 10, 0],\n  [2, 5, 0, 3, 8],\n  [0, 10, 3, 0, 6],\n  [0, 0, 8, 6, 0]\n];\ndijkstra(graph, 0, 4);\n// Output: Distance to target: 8",
      "python": "def dijkstra(graph, start, target):\n    vertices = len(graph)\n    dist = [float('inf')] * vertices\n    visited = [False] * vertices\n    dist[start] = 0\n    for count in range(vertices - 1):\n        min_dist, u = float('inf'), 0\n        for i in range(vertices):\n            if not visited[i] and dist[i] <= min_dist:\n                min_dist, u = dist[i], i\n        visited[u] = True\n        for v in range(vertices):\n            if not visited[v] and graph[u][v] and dist[u] != float('inf') and dist[u] + graph[u][v] < dist[v]:\n                dist[v] = dist[u] + graph[u][v]\n    if dist[target] == float('inf'):\n        print(\"Target not reachable\")\n    else:\n        print(f\"Distance to target: {dist[target]}\")\n\ngraph = [\n    [0, 4, 2, 0, 0],\n    [4, 0, 5, 10, 0],\n    [2, 5, 0, 3, 8],\n    [0, 10, 3, 0, 6],\n    [0, 0, 8, 6, 0]\n]\ndijkstra(graph, 0, 4)\n# Output: Distance to target: 8",
      "php": "<?php\n\nfunction dijkstra($graph, $start, $target) {\n  $vertices = count($graph);\n  $dist = array_fill(0, $vertices, PHP_INT_MAX);\n  $visited = array_fill(0, $vertices, false);\n  $dist[$start] = 0;\n  for ($count = 0; $count < $vertices - 1; $count++) {\n    $min = PHP_INT_MAX;\n    $u = 0;\n    for ($i = 0; $i < $vertices; $i++) {\n      if (!$visited[$i] && $dist[$i] <= $min) {\n        $min = $dist[$i];\n        $u = $i;\n      }\n    }\n    $visited[$u] = true;\n    for ($v = 0; $v < $vertices; $v++) {\n      if (!$visited[$v] && $graph[$u][$v] && $dist[$u] != PHP_INT_MAX && $dist[$u] + $graph[$u][$v] < $dist[$v]) {\n        $dist[$v] = $dist[$u] + $graph[$u][$v];\n      }\n    }\n  }\n  if ($dist[$target] == PHP_INT_MAX) {\n    echo \"Target not reachable\\n\";\n  } else {\n    echo \"Distance to target: \" . $dist[$target] . \"\\n\";\n  }\n}\n\n$graph = [\n  [0, 4, 2, 0, 0],\n  [4, 0, 5, 10, 0],\n  [2, 5, 0, 3, 8],\n  [0, 10, 3, 0, 6],\n  [0, 0, 8, 6, 0]\n];\ndijkstra($graph, 0, 4);\n// Output: Distance to target: 8"
    },
    "content": [
      {
        "type": "heading",
        "text": "Что такое алгоритмы поиска кратчайшего пути?"
      },
      {
        "type": "paragraph",
        "text": "Алгоритмы поиска кратчайшего пути находят кратчайший путь между двумя вершинами во взвешенном графе, где рёбра имеют веса (например, расстояния между городами). Граф состоит из вершин и взвешенных рёбер. Наиболее известные алгоритмы — Dijkstra и Bellman-Ford. Алгоритм Dijkstra находит кратчайший путь в графах с неотрицательными весами, используя жадный подход. Bellman-Ford поддерживает отрицательные веса и может обнаруживать отрицательные циклы. Например, Dijkstra может найти кратчайшее расстояние между двумя городами (например, от вершины 0 до вершины 4, расстояние 8). По сравнению с бэктрекингом, алгоритмы кратчайшего пути эффективнее, так как не исследуют все возможные пути. По сравнению с динамическим программированием, Dijkstra использует меньше памяти, но Bellman-Ford может требовать больше вычислений."
      },
      {
        "type": "heading",
        "text": "Как работают алгоритмы поиска кратчайшего пути?"
      },
      {
        "type": "paragraph",
        "text": "Алгоритм Dijkstra работает следующим образом: 1) **Начальная вершина** — поиск начинается с начальной вершины (например, вершины 0). 2) **Массив расстояний** — создаётся массив для хранения кратчайших расстояний до каждой вершины, с начальной вершиной равной 0 и остальными равными бесконечности. 3) **Жадный выбор** — на каждом шаге выбирается непосещённая вершина с наименьшим расстоянием. 4) **Обновление расстояний** — расстояния до соседей выбранной вершины обновляются, если найден более короткий путь. Например, в графе с 5 вершинами Dijkstra находит путь от вершины 0 до вершины 4 (например, 0 → 2 → 4, расстояние 8). Bellman-Ford итеративно проверяет все рёбра и обрабатывает отрицательные веса. Dijkstra быстрее для неотрицательных весов, тогда как Bellman-Ford подходит для отрицательных весов и обнаружения циклов."
      },
      {
        "type": "heading",
        "text": "Эффективность алгоритмов поиска кратчайшего пути (Big O)"
      },
      {
        "type": "paragraph",
        "text": "Эффективность алгоритма Dijkstra зависит от представления графа. С матрицей смежности временная сложность составляет O(V²), где V — количество вершин, так как проверяются все соседи. С приоритетной очередью (heap) — O((V + E) log V), где E — количество рёбер. Например, в графе с 5 вершинами и 7 рёбрами Dijkstra занимает O(25) или O(12 log 5) операций с heap. Временная сложность Bellman-Ford — O(V * E), что может быть медленно для больших графов. Пространственная сложность для обоих — O(V) для массивов расстояний и посещений. По сравнению с бэктрекингом (например, O(N!) для задачи N ферзей), алгоритмы кратчайшего пути эффективнее. По сравнению с динамическим программированием (например, O(n * m) для задачи о минимальном количестве монет), Dijkstra использует меньше памяти, но Bellman-Ford может требовать больше вычислений."
      },
      {
        "type": "heading",
        "text": "Преимущества и недостатки алгоритмов поиска кратчайшего пути"
      },
      {
        "type": "list",
        "items": [
          {
            "description": "Эффективность: Dijkstra быстр для графов с неотрицательными весами."
          },
          {
            "description": "Универсальность: Bellman-Ford поддерживает отрицательные веса и обнаружение циклов."
          },
          {
            "description": "Недостаток: Dijkstra не работает с отрицательными весами."
          },
          {
            "description": "Недостаток: Bellman-Ford медлен для больших графов (O(V * E))."
          }
        ]
      },
      {
        "type": "heading",
        "text": "Алгоритмы поиска кратчайшего пути в реальной жизни"
      },
      {
        "type": "paragraph",
        "text": "Алгоритмы поиска кратчайшего пути широко применяются в навигационных системах (например, Google Maps для нахождения кратчайшего маршрута между двумя точками), сетевой маршрутизации (например, отправка пакетов данных по кратчайшему пути в интернете) и логистике (например, оптимизация маршрутов доставки). Dijkstra эффективен для графов с неотрицательными весами, таких как расстояния между городами. Bellman-Ford используется в сценариях с отрицательными весами, например, в арбитраже обмена валют. По сравнению с бэктрекингом, алгоритмы кратчайшего пути эффективнее, так как не исследуют все пути. По сравнению с динамическим программированием, Dijkstra использует меньше памяти, но Bellman-Ford может требовать больше вычислений. Эти алгоритмы важны в современных технологиях, таких как GPS и сетевая безопасность."
      },
      {
        "type": "heading",
        "text": "Опасности и рекомендации"
      },
      {
        "type": "paragraph",
        "text": "Основная опасность алгоритмов поиска кратчайшего пути — вычислительная нагрузка для больших графов. Dijkstra с матрицей смежности требует O(V²) времени, а Bellman-Ford с O(V * E) может быть медленнее. Использование памяти также может быть проблемой для больших графов. Рекомендации включают использование приоритетной очереди для Dijkstra для достижения эффективности O((V + E) log V). Для графов с отрицательными весами используйте Bellman-Ford. Выбирайте алгоритм в зависимости от задачи, например, Dijkstra для неотрицательных весов, Bellman-Ford для отрицательных весов или обнаружения циклов. Тестируйте алгоритм с графами разного размера для оценки производительности. Если скорость в приоритете, исследуйте эвристические алгоритмы, такие как A*, но проверяйте, дают ли они оптимальные решения."
      }
    ]
  }
}
