{
  "az": {
    "title": "TypeScript Basic Types",
    "description": "TypeScript-də basic types dəyişənlərə əsas tiplər təyin edərək kodun daha təhlükəsiz və oxunaqlı olmasını təmin edir. Bu tiplərə string, number, boolean, any, array, tuple, void, null və undefined daxildir.",
    "content": [
      {
        "type": "heading",
        "text": "Basic Types nədir?"
      },
      {
        "type": "paragraph",
        "text": "TypeScript-də basic types dəyişənlərin hansı dəyərləri qəbul edəcəyini təyin etmək üçün istifadə olunan əsas tiplərdir. Bu tiplər `:` simvolu ilə dəyişənlərə təyin edilir. Məsələn, `let name: string = \"Murad\";` dəyişənin yalnız sətir olacağını bildirir. Basic types sayəsində səhv dəyərlər istifadə edildikdə TypeScript xəbərdarlıq verir, bu da kodun səhvsiz olmasını təmin edir."
      },
      {
        "type": "heading",
        "text": "Basic Types necə işləyir?"
      },
      {
        "type": "paragraph",
        "text": "Basic types TypeScript-də kodun düzgün işləməsini təmin etmək üçün dəyişənlərə və funksiyalara təyin edilir. Ən çox istifadə olunan basic types bunlardır: `string` (sətirlər), `number` (rəqəmlər), `boolean` (true/false), `any` (hər hansı tip), `array` (siyahılar), `tuple` (sabit uzunluqlu siyahılar), `void` (heç bir dəyər qaytarmayan funksiyalar), `null` və `undefined` (boş və ya təyin edilməmiş dəyərlər). Əgər səhv tip istifadə etsən, TypeScript səni xəbərdar edər."
      },
      {
        "type": "code",
        "language": "typescript",
        "code": "// 1. string: Sətir dəyərlər\nlet userName: string = \"Murad\"; // Yalnız sətir\n// userName = 23; // Xəta: number string tipinə uyğun deyil\n\n// 2. number: Rəqəmlər (tam və ya onluq)\nlet userAge: number = 23; // Yalnız rəqəm\n// userAge = \"yirmi üç\"; // Xəta: string number tipinə uyğun deyil\n\n// 3. boolean: True və ya False\nlet isActive: boolean = true; // Yalnız true/false\n// isActive = \"yes\"; // Xəta: string boolean tipinə uyğun deyil\n\n// 4. any: Hər hansı tip\nlet data: any = \"test\"; // Sətir ola bilər\ndata = 42; // Rəqəm də ola bilər\ndata = true; // Hər hansı dəyər qəbul edir\n\n// 5. array: Eyni tipdə dəyərlər siyahısı\nlet prices: number[] = [10, 20, 30]; // Yalnız rəqəmlər\n// prices.push(\"40\"); // Xəta: string number[] tipinə uyğun deyil\n\n// 6. tuple: Sabit uzunluqlu siyahı\ntype UserInfo = [string, number];\nlet user: UserInfo = [\"Alice\", 25]; // Ad və yaş\n// user = [25, \"Alice\"]; // Xəta: tiplər düzgün sırayla deyil\n\n// 7. void: Heç bir dəyər qaytarmayan funksiya\nfunction logMessage(message: string): void {\n  console.log(message); // Heç bir dəyər qaytarmır\n}\n\n// 8. null və undefined\nlet emptyValue: null = null; // Yalnız null\nlet notDefined: undefined = undefined; // Yalnız undefined"
      },
      {
        "type": "heading",
        "text": "Real həyatda istifadə ssenariləri"
      },
      {
        "type": "paragraph",
        "text": "Basic types real layihələrdə çox faydalıdır. Məsələn, bir onlayn mağazada məhsulun adını (string), qiymətini (number) və ya mövcudluğunu (boolean) təyin etmək üçün istifadə edə bilərsən. Və ya bir istifadəçi profilində ad və yaşı tuple ilə saxlaya bilərsən. Aşağıda sadə bir mağaza nümunəsi var, burada basic types istifadə olunur."
      },
      {
        "type": "code",
        "language": "typescript",
        "code": "// Sadə mağaza sistemi\nlet productName: string = \"Laptop\"; // Məhsul adı\nlet productPrice: number = 1200; // Məhsul qiyməti\nlet inStock: boolean = true; // Mövcudluq\nlet productIds: number[] = [101, 102, 103]; // Məhsul ID-ləri\n\ntype ProductInfo = [string, number]; // Məhsul adı və qiyməti\nlet product: ProductInfo = [\"Mouse\", 25];\n\nfunction showProduct(name: string, price: number): string {\n  return `${name}: $${price}`;\n}\n\nconsole.log(showProduct(productName, productPrice)); // Çıxış: Laptop: $1200\nconsole.log(showProduct(product[0], product[1])); // Çıxış: Mouse: $25"
      },
      {
        "type": "heading",
        "text": "Basic Types-in məqsədi"
      },
      {
        "type": "paragraph",
        "text": "Basic types TypeScript-in əsas xüsusiyyətidir və JavaScript-in zəifliklərini aradan qaldırır. JavaScript-də dəyişənlərin tipi əvvəlcədən bilinmir, bu da səhvlərə səbəb ola bilər. TypeScript-də basic types ilə dəyişənlərin tipini dəqiqləşdirərək səhvləri kod yazarkən tapmaq mümkündür. Bu, Microsoft, Google və Airbnb kimi şirkətlərin böyük layihələrində kodun keyfiyyətini artırır."
      },
      {
        "type": "heading",
        "text": "Əsas xüsusiyyətlər"
      },
      {
        "type": "list",
        "items": [
          {
            "term": "string",
            "description": "Sətir dəyərlər, məsələn, `\"Murad\"`."
          },
          {
            "term": "number",
            "description": "Tam və ya onluq rəqəmlər, məsələn, `23` və ya `3.14`."
          },
          {
            "term": "boolean",
            "description": "Yalnız `true` və ya `false` dəyərləri."
          },
          {
            "term": "any",
            "description": "Hər hansı tipdə dəyər qəbul edir, lakin ehtiyatla istifadə edilməlidir."
          },
          {
            "term": "array",
            "description": "Eyni tipdə dəyərlər siyahısı, məsələn, `number[]`."
          },
          {
            "term": "tuple",
            "description": "Sabit uzunluqlu və müxtəlif tipli siyahılar, məsələn, `[string, number]`."
          },
          {
            "term": "void",
            "description": "Heç bir dəyər qaytarmayan funksiyalar üçün."
          },
          {
            "term": "null və undefined",
            "description": "Boş və ya təyin edilməmiş dəyərlər."
          }
        ]
      },
      {
        "type": "heading",
        "text": "Təhlükələr və tövsiyələr"
      },
      {
        "type": "paragraph",
        "text": "- **Təhlükələr**: `any` tipinin çox istifadəsi TypeScript-in tipləmə üstünlüklərini azaldır, çünki bu, JavaScript kimi davranır. Çox sayda fərqli tipdən istifadə kodu mürəkkəbləşdirə bilər.\n- **Tövsiyələr**: `any` tipindən qaçın, sadə və dəqiq tiplər istifadə et, tuple və array-ləri düzgün təyin et, səhvləri vaxtında tapmaq üçün VSCode kimi IDE-dən faydalan."
      },
      {
        "type": "heading",
        "text": "Sınaq üçün nümunə"
      },
      {
        "type": "editor",
        "initialCode": "// Sadə basic types nümunəsi\nlet itemName: string = \"Mouse\"; // Sətir\nlet itemPrice: number = 25; // Rəqəm\nlet inStock: boolean = true; // True/False\n\ntype ProductInfo = [string, number]; // Tuple\nlet product: ProductInfo = [\"Laptop\", 1200];\n\nfunction showItem(name: string, price: number): string {\n  return `${name}: $${price}`;\n}\n\nconsole.log(showItem(itemName, itemPrice)); // Çıxış: Mouse: $25\nconsole.log(showItem(product[0], product[1])); // Çıxış: Laptop: $1200"
      }
    ]
  },
  "en": {
    "title": "TypeScript Basic Types",
    "description": "Basic Types in TypeScript allow you to define the types of variables, making code safer and more readable. They include string, number, boolean, any, array, tuple, void, null, and undefined.",
    "content": [
      {
        "type": "heading",
        "text": "What are Basic Types?"
      },
      {
        "type": "paragraph",
        "text": "Basic Types in TypeScript are the fundamental types used to specify what values a variable can hold. They are defined using the `:` symbol. For example, `let name: string = \"Murad\";` ensures the variable only holds strings. Basic types help TypeScript catch errors when incorrect values are used, making code more reliable."
      },
      {
        "type": "heading",
        "text": "How do Basic Types work?"
      },
      {
        "type": "paragraph",
        "text": "Basic Types are assigned to variables and functions to ensure correct behavior. The most common basic types are: `string` (text), `number` (numbers), `boolean` (true/false), `any` (any type), `array` (lists), `tuple` (fixed-length lists), `void` (functions returning nothing), `null`, and `undefined` (empty or undefined values). TypeScript warns you if you use an incorrect type."
      },
      {
        "type": "code",
        "language": "typescript",
        "code": "// 1. string: Text values\nlet userName: string = \"Murad\"; // Only string\n// userName = 23; // Error: number is not string type\n\n// 2. number: Integer or decimal numbers\nlet userAge: number = 23; // Only number\n// userAge = \"twenty three\"; // Error: string is not number type\n\n// 3. boolean: True or False\nlet isActive: boolean = true; // Only true/false\n// isActive = \"yes\"; // Error: string is not boolean type\n\n// 4. any: Any type\nlet data: any = \"test\"; // Can be string\ndata = 42; // Can be number\ndata = true; // Accepts any value\n\n// 5. array: List of same-type values\nlet prices: number[] = [10, 20, 30]; // Only numbers\n// prices.push(\"40\"); // Error: string is not number[] type\n\n// 6. tuple: Fixed-length array\ntype UserInfo = [string, number];\nlet user: UserInfo = [\"Alice\", 25]; // Name and age\n// user = [25, \"Alice\"]; // Error: types are in wrong order\n\n// 7. void: Function returning no value\nfunction logMessage(message: string): void {\n  console.log(message); // Returns nothing\n}\n\n// 8. null and undefined\nlet emptyValue: null = null; // Only null\nlet notDefined: undefined = undefined; // Only undefined"
      },
      {
        "type": "heading",
        "text": "Real-world use cases"
      },
      {
        "type": "paragraph",
        "text": "Basic types are very useful in real projects. For example, in an online store, you can use them to define a product’s name (string), price (number), or availability (boolean). You could also use a tuple to store a user’s name and age together. Below is a simple store example using basic types."
      },
      {
        "type": "code",
        "language": "typescript",
        "code": "// Simple store system\nlet productName: string = \"Laptop\"; // Product name\nlet productPrice: number = 1200; // Product price\nlet inStock: boolean = true; // Availability\nlet productIds: number[] = [101, 102, 103]; // Product IDs\n\ntype ProductInfo = [string, number]; // Product name and price\nlet product: ProductInfo = [\"Mouse\", 25];\n\nfunction showProduct(name: string, price: number): string {\n  return `${name}: $${price}`;\n}\n\nconsole.log(showProduct(productName, productPrice)); // Output: Laptop: $1200\nconsole.log(showProduct(product[0], product[1])); // Output: Mouse: $25"
      },
      {
        "type": "heading",
        "text": "Purpose of Basic Types"
      },
      {
        "type": "paragraph",
        "text": "Basic types are a core feature of TypeScript, addressing JavaScript’s lack of type safety. In JavaScript, variable types are unknown until runtime, which can cause errors. Basic types in TypeScript allow you to define types upfront, catching errors during development. This is used in large projects by companies like Microsoft, Google, and Airbnb to improve code quality."
      },
      {
        "type": "heading",
        "text": "Key features"
      },
      {
        "type": "list",
        "items": [
          {
            "term": "string",
            "description": "Text values, e.g., `\"Murad\"`."
          },
          {
            "term": "number",
            "description": "Integer or decimal numbers, e.g., `23` or `3.14`."
          },
          {
            "term": "boolean",
            "description": "Only `true` or `false` values."
          },
          {
            "term": "any",
            "description": "Accepts any type, but should be used sparingly."
          },
          {
            "term": "array",
            "description": "Lists of values of the same type, e.g., `number[]`."
          },
          {
            "term": "tuple",
            "description": "Fixed-length arrays with different types, e.g., `[string, number]`."
          },
          {
            "term": "void",
            "description": "For functions that return no value."
          },
          {
            "term": "null and undefined",
            "description": "Empty or undefined values."
          }
        ]
      },
      {
        "type": "heading",
        "text": "Dangers and recommendations"
      },
      {
        "type": "paragraph",
        "text": "- **Dangers**: Overusing `any` reduces TypeScript’s benefits, as it bypasses type checking. Using too many different types can make code complex.\n- **Recommendations**: Avoid `any`, use simple and precise types, define arrays and tuples correctly, and use an IDE like VSCode to catch errors early."
      },
      {
        "type": "heading",
        "text": "Try it yourself"
      },
      {
        "type": "editor",
        "initialCode": "// Simple basic types example\nlet itemName: string = \"Mouse\"; // String\nlet itemPrice: number = 25; // Number\nlet inStock: boolean = true; // True/False\n\ntype ProductInfo = [string, number]; // Tuple\nlet product: ProductInfo = [\"Laptop\", 1200];\n\nfunction showItem(name: string, price: number): string {\n  return `${name}: $${price}`;\n}\n\nconsole.log(showItem(itemName, itemPrice)); // Output: Mouse: $25\nconsole.log(showItem(product[0], product[1])); // Output: Laptop: $1200"
      }
    ]
  },
  "ru": {
    "title": "TypeScript Basic Types",
    "description": "Basic Types в TypeScript позволяют задавать типы переменных, делая код более безопасным и читаемым. К ним относятся string, number, boolean, any, array, tuple, void, null и undefined.",
    "content": [
      {
        "type": "heading",
        "text": "Что такое Basic Types?"
      },
      {
        "type": "paragraph",
        "text": "Basic Types в TypeScript — это основные типы, используемые для указания, какие значения может принимать переменная. Они задаются с помощью символа `:`. Например, `let name: string = \"Murad\";` гарантирует, что переменная принимает только строки. Basic types помогают TypeScript находить ошибки при использовании неверных значений, делая код надёжнее."
      },
      {
        "type": "heading",
        "text": "Как работают Basic Types?"
      },
      {
        "type": "paragraph",
        "text": "Basic Types назначаются переменным и функциям для обеспечения правильной работы кода. Наиболее распространённые basic types: `string` (строки), `number` (числа), `boolean` (true/false), `any` (любой тип), `array` (списки), `tuple` (массивы фиксированной длины), `void` (функции, не возвращающие значения), `null` и `undefined` (пустые или неопределённые значения). TypeScript предупреждает, если используется неверный тип."
      },
      {
        "type": "code",
        "language": "typescript",
        "code": "// 1. string: Строковые значения\nlet userName: string = \"Murad\"; // Только строка\n// userName = 23; // Ошибка: число не соответствует типу string\n\n// 2. number: Целые или дробные числа\nlet userAge: number = 23; // Только число\n// userAge = \"двадцать три\"; // Ошибка: строка не соответствует типу number\n\n// 3. boolean: True или False\nlet isActive: boolean = true; // Только true/false\n// isActive = \"yes\"; // Ошибка: строка не соответствует типу boolean\n\n// 4. any: Любой тип\nlet data: any = \"test\"; // Может быть строкой\ndata = 42; // Может быть числом\ndata = true; // Принимает любое значение\n\n// 5. array: Список значений одного типа\nlet prices: number[] = [10, 20, 30]; // Только числа\n// prices.push(\"40\"); // Ошибка: строка не соответствует типу number[]\n\n// 6. tuple: Массив фиксированной длины\ntype UserInfo = [string, number];\nlet user: UserInfo = [\"Alice\", 25]; // Имя и возраст\n// user = [25, \"Alice\"]; // Ошибка: типы в неправильном порядке\n\n// 7. void: Функция, не возвращающая значение\nfunction logMessage(message: string): void {\n  console.log(message); // Ничего не возвращает\n}\n\n// 8. null и undefined\nlet emptyValue: null = null; // Только null\nlet notDefined: undefined = undefined; // Только undefined"
      },
      {
        "type": "heading",
        "text": "Примеры из реальной жизни"
      },
      {
        "type": "paragraph",
        "text": "Basic types очень полезны в реальных проектах. Например, в интернет-магазине они используются для указания имени товара (string), цены (number) или наличия (boolean). Также можно использовать tuple для хранения имени и возраста пользователя. Ниже приведён простой пример магазина с использованием basic types."
      },
      {
        "type": "code",
        "language": "typescript",
        "code": "// Простая система магазина\nlet productName: string = \"Laptop\"; // Название товара\nlet productPrice: number = 1200; // Цена товара\nlet inStock: boolean = true; // Наличие\nlet productIds: number[] = [101, 102, 103]; // ID товаров\n\ntype ProductInfo = [string, number]; // Название и цена товара\nlet product: ProductInfo = [\"Mouse\", 25];\n\nfunction showProduct(name: string, price: number): string {\n  return `${name}: $${price}`;\n}\n\nconsole.log(showProduct(productName, productPrice)); // Вывод: Laptop: $1200\nconsole.log(showProduct(product[0], product[1])); // Вывод: Mouse: $25"
      },
      {
        "type": "heading",
        "text": "Цель Basic Types"
      },
      {
        "type": "paragraph",
        "text": "Basic types — ключевая особенность TypeScript, устраняющая недостатки JavaScript. В JavaScript типы переменных неизвестны до выполнения кода, что может привести к ошибкам. Basic types в TypeScript позволяют заранее указывать типы, обнаруживая ошибки на этапе разработки. Это используется в крупных проектах компаниями, такими как Microsoft, Google и Airbnb, для повышения качества кода."
      },
      {
        "type": "heading",
        "text": "Основные особенности"
      },
      {
        "type": "list",
        "items": [
          {
            "term": "string",
            "description": "Строковые значения, например, `\"Murad\"`."
          },
          {
            "term": "number",
            "description": "Целые или дробные числа, например, `23` или `3.14`."
          },
          {
            "term": "boolean",
            "description": "Только значения `true` или `false`."
          },
          {
            "term": "any",
            "description": "Принимает любой тип, но использовать нужно осторожно."
          },
          {
            "term": "array",
            "description": "Списки значений одного типа, например, `number[]`."
          },
          {
            "term": "tuple",
            "description": "Массивы фиксированной длины с разными типами, например, `[string, number]`."
          },
          {
            "term": "void",
            "description": "Для функций, не возвращающих значения."
          },
          {
            "term": "null и undefined",
            "description": "Пустые или неопределённые значения."
          }
        ]
      },
      {
        "type": "heading",
        "text": "Опасности и рекомендации"
      },
      {
        "type": "paragraph",
        "text": "- **Опасности**: Чрезмерное использование `any` снижает преимущества TypeScript, так как отключает проверку типов. Использование множества разных типов может усложнить код.\n- **Рекомендации**: Избегайте `any`, используйте простые и точные типы, правильно определяйте массивы и tuple, используйте IDE, такие как VSCode, для раннего обнаружения ошибок."
      },
      {
        "type": "heading",
        "text": "Попробуй сам"
      },
      {
        "type": "editor",
        "initialCode": "// Простой пример basic types\nlet itemName: string = \"Mouse\"; // Строка\nlet itemPrice: number = 25; // Число\nlet inStock: boolean = true; // True/False\n\ntype ProductInfo = [string, number]; // Tuple\nlet product: ProductInfo = [\"Laptop\", 1200];\n\nfunction showItem(name: string, price: number): string {\n  return `${name}: $${price}`;\n}\n\nconsole.log(showItem(itemName, itemPrice)); // Вывод: Mouse: $25\nconsole.log(showItem(product[0], product[1])); // Вывод: Laptop: $1200"
      }
    ]
  }
}
