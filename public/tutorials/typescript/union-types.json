{
    "az": {
      "title": "TypeScript Union Types",
      "description": "TypeScript-də union types bir dəyişənin birdən çox tipdən biri ola biləcəyini təyin edir, çeviklik və tip təhlükəsizliyini təmin edir.",
      "content": [
        {
          "type": "heading",
          "text": "Union Types nədir?"
        },
        {
          "type": "paragraph",
          "text": "TypeScript-də union types bir dəyişənin bir neçə fərqli tipdən biri ola biləcəyini göstərir. Bu, `|` simvolu ilə təyin edilir. Məsələn, `string | number` dəyişənin ya sətir, ya da rəqəm ola biləcəyini bildirir."
        },
        {
          "type": "heading",
          "text": "Union Types necə işləyir?"
        },
        {
          "type": "paragraph",
          "text": "Union types `:` simvolu ilə dəyişənə və ya parametrə təyin edilir. Dəyişən yalnız təyin edilmiş tiplərdən biri ola bilər. Səhv tip istifadə etsən, TypeScript xəbərdarlıq verər. Aşağıda kod nümunələri ilə baxaq."
        },
        {
          "type": "code",
          "language": "typescript",
          "code": "// 1. Sadə union type\nlet result: string | number;\nresult = \"Success\"; // Düzgündür\nresult = 42; // Düzgündür\n// result = true; // Xəta: boolean string | number tipinə uyğun deyil\n\n// 2. Funksiya ilə union type\nfunction printValue(value: string | number): void {\n  console.log(`Dəyər: ${value}`);\n}\n\n// 3. Interface ilə union type\ninterface Item {\n  name: string;\n  price: number;\n}\nlet data: Item | null;\ndata = { name: \"Shirt\", price: 50 }; // Düzgündür\ndata = null; // Düzgündür\n\n// 4. Union type ilə tip daraltma\nfunction processInput(input: string | number): string {\n  if (typeof input === \"string\") {\n    return input.toUpperCase();\n  }\n  return `Number: ${input * 2}`;\n}\n\n// 5. Array ilə union type\nlet mixedList: (string | number)[] = [\"Shirt\", 50, \"Jacket\", 100];"
        },
        {
          "type": "heading",
          "text": "Əsas xüsusiyyətlər"
        },
        {
          "type": "list",
          "items": [
            {
              "term": "Çoxsaylı tiplər",
              "description": "Bir dəyişən bir neçə tipdən biri ola bilər."
            },
            {
              "term": "Tip daraltma",
              "description": "`typeof` və ya `if` ilə tiplər dəqiqləşdirilə bilər."
            },
            {
              "term": "Funksiyalarda istifadə",
              "description": "Parametrlər və ya qaytarma dəyərləri union types ilə təyin edilə bilər."
            },
            {
              "term": "Array ilə birləşmə",
              "description": "Union types array elementləri üçün istifadə oluna bilər."
            }
          ]
        },
        {
          "type": "heading",
          "text": "Təhlükələr və tövsiyələr"
        },
        {
          "type": "paragraph",
          "text": "- **Təhlükələr**: Çox sayda union type istifadəsi kodu mürəkkəbləşdirə bilər. `any` ilə qarışdırmaq TypeScript-in faydasını azaldır.\n- **Tövsiyələr**: Mümkün qədər az union type istifadə et, tip daraltma ilə dəqiqləşdir, `any` tipindən qaçın."
        },
        {
          "type": "heading",
          "text": "Sınaq üçün nümunə"
        },
        {
          "type": "editor",
          "initialCode": "// Öz union types nümunəni yarat!\ninterface Item {\n  name: string;\n  price: number;\n}\n\nfunction processItem(data: Item | string): void {\n  if (typeof data === \"string\") {\n    console.log(`String: ${data}`);\n  } else {\n    console.log(`${data.name}: $${data.price}`);\n  }\n}\n\nconst item: Item = { name: \"Shirt\", price: 50 };\nprocessItem(item);\nprocessItem(\"Jacket\");"
        }
      ]
    },
    "en": {
      "title": "TypeScript Union Types",
      "description": "Union Types in TypeScript allow a variable to be one of several types, providing flexibility and type safety.",
      "content": [
        {
          "type": "heading",
          "text": "What are Union Types?"
        },
        {
          "type": "paragraph",
          "text": "Union Types in TypeScript let a variable be one of multiple types, defined using the `|` symbol. For example, `string | number` means a variable can be either a string or a number."
        },
        {
          "type": "heading",
          "text": "How do Union Types work?"
        },
        {
          "type": "paragraph",
          "text": "Union Types are assigned using the `:` symbol. A variable can only hold one of the specified types. Using an incorrect type triggers a TypeScript warning. See code examples below."
        },
        {
          "type": "code",
          "language": "typescript",
          "code": "// 1. Simple union type\nlet result: string | number;\nresult = \"Success\"; // Valid\nresult = 42; // Valid\n// result = true; // Error: boolean is not string | number\n\n// 2. Function with union type\nfunction printValue(value: string | number): void {\n  console.log(`Value: ${value}`);\n}\n\n// 3. Interface with union type\ninterface Item {\n  name: string;\n  price: number;\n}\nlet data: Item | null;\ndata = { name: \"Shirt\", price: 50 }; // Valid\ndata = null; // Valid\n\n// 4. Type narrowing with union type\nfunction processInput(input: string | number): string {\n  if (typeof input === \"string\") {\n    return input.toUpperCase();\n  }\n  return `Number: ${input * 2}`;\n}\n\n// 5. Union type with array\nlet mixedList: (string | number)[] = [\"Shirt\", 50, \"Jacket\", 100];"
        },
        {
          "type": "heading",
          "text": "Key features"
        },
        {
          "type": "list",
          "items": [
            {
              "term": "Multiple types",
              "description": "A variable can be one of several types."
            },
            {
              "term": "Type narrowing",
              "description": "Use `typeof` or `if` to specify types."
            },
            {
              "term": "Use in functions",
              "description": "Parameters or return values can use union types."
            },
            {
              "term": "Array integration",
              "description": "Union types can be used for array elements."
            }
          ]
        },
        {
          "type": "heading",
          "text": "Dangers and recommendations"
        },
        {
          "type": "paragraph",
          "text": "- **Dangers**: Using too many union types can complicate code. Mixing with `any` reduces TypeScript’s benefits.\n- **Recommendations**: Use minimal union types, narrow types when possible, and avoid `any`."
        },
        {
          "type": "heading",
          "text": "Try it yourself"
        },
        {
          "type": "editor",
          "initialCode": "// Create your own union types example!\ninterface Item {\n  name: string;\n  price: number;\n}\n\nfunction processItem(data: Item | string): void {\n  if (typeof data === \"string\") {\n    console.log(`String: ${data}`);\n  } else {\n    console.log(`${data.name}: $${data.price}`);\n  }\n}\n\nconst item: Item = { name: \"Shirt\", price: 50 };\nprocessItem(item);\nprocessItem(\"Jacket\");"
        }
      ]
    },
    "ru": {
      "title": "TypeScript Union Types",
      "description": "Union Types в TypeScript позволяют переменной быть одним из нескольких типов, обеспечивая гибкость и безопасность типов.",
      "content": [
        {
          "type": "heading",
          "text": "Что такое Union Types?"
        },
        {
          "type": "paragraph",
          "text": "Union Types в TypeScript позволяют переменной быть одним из нескольких типов, задаваемых с помощью символа `|`. Например, `string | number` означает, что переменная может быть строкой или числом."
        },
        {
          "type": "heading",
          "text": "Как работают Union Types?"
        },
        {
          "type": "paragraph",
          "text": "Union Types задаются с помощью символа `:`. Переменная может содержать только один из указанных типов. Использование неверного типа вызывает предупреждение TypeScript. Рассмотрим примеры кода."
        },
        {
          "type": "code",
          "language": "typescript",
          "code": "// 1. Простой union type\nlet result: string | number;\nresult = \"Success\"; // Корректно\nresult = 42; // Корректно\n// result = true; // Ошибка: boolean не является string | number\n\n// 2. Функция с union type\nfunction printValue(value: string | number): void {\n  console.log(`Значение: ${value}`);\n}\n\n// 3. Interface с union type\ninterface Item {\n  name: string;\n  price: number;\n}\nlet data: Item | null;\ndata = { name: \"Shirt\", price: 50 }; // Корректно\ndata = null; // Корректно\n\n// 4. Сужение типа с union type\nfunction processInput(input: string | number): string {\n  if (typeof input === \"string\") {\n    return input.toUpperCase();\n  }\n  return `Число: ${input * 2}`;\n}\n\n// 5. Union type с массивом\nlet mixedList: (string | number)[] = [\"Shirt\", 50, \"Jacket\", 100];"
        },
        {
          "type": "heading",
          "text": "Основные особенности"
        },
        {
          "type": "list",
          "items": [
            {
              "term": "Множественные типы",
              "description": "Переменная может быть одним из нескольких типов."
            },
            {
              "term": "Сужение типов",
              "description": "Используй `typeof` или `if` для уточнения типов."
            },
            {
              "term": "Использование в функциях",
              "description": "Параметры или возвращаемые значения могут использовать union types."
            },
            {
              "term": "Интеграция с массивами",
              "description": "Union types можно использовать для элементов массива."
            }
          ]
        },
        {
          "type": "heading",
          "text": "Опасности и рекомендации"
        },
        {
          "type": "paragraph",
          "text": "- **Опасности**: Слишком много union types могут усложнить код. Смешивание с `any` снижает пользу TypeScript.\n- **Рекомендации**: Используй минимум union types, уточняй типы когда возможно, избегай `any`."
        },
        {
          "type": "heading",
          "text": "Попробуй сам"
        },
        {
          "type": "editor",
          "initialCode": "// Создай свой пример с union types!\ninterface Item {\n  name: string;\n  price: number;\n}\n\nfunction processItem(data: Item | string): void {\n  if (typeof data === \"string\") {\n    console.log(`Строка: ${data}`);\n  } else {\n    console.log(`${data.name}: $${data.price}`);\n  }\n}\n\nconst item: Item = { name: \"Shirt\", price: 50 };\nprocessItem(item);\nprocessItem(\"Jacket\");"
        }
      ]
    }
  }